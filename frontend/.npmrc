# pnpm (v10.16+/11.x) blocks installing any package published within the
# last 24h by default, as a supply-chain-attack guard. Our pnpm-lock.yaml is
# a committed, reviewed file — the freshness check adds no protection here
# and just breaks builds when a legitimate dependency update lands recently.
minimum-release-age=0
