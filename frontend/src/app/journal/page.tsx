import { PhaseStub } from "@/components/ui/phase-stub";

export default function Page() {
  return <PhaseStub title="יומן מסחר" phase={7} items={["רשומות יומן, תזות, תגיות, Templates וניתוח משמעת"]} />;
}
